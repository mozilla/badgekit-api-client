# Issuing

The [`Client`](methods.md) object provides the following methods:

* **`getBadgeInstances`**
* **`getBadgeInstance`**
* **`createBadgeInstance`**
* **`deleteBadgeInstance`**
* **`updateBadgeInstance`**
